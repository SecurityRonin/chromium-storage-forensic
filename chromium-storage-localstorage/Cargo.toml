[package]
name = "chromium-storage-localstorage"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Chromium storage localstorage reader — backward-compatible facade re-exporting chromium-storage-localstorage-core (split into a -core reader + a -forensic analyzer). New consumers should depend on chromium-storage-localstorage-core directly."
repository.workspace = true
authors.workspace = true
readme = "README.md"
keywords = ["forensics", "chromium", "localstorage", "dfir", "browser"]
categories = ["parser-implementations"]

[lib]
name = "chromium_storage_localstorage"
path = "src/lib.rs"

[dependencies]
chromium-storage-localstorage-core = { workspace = true }

[lints]
workspace = true
